export interface OptionsCode {
    subdirectory: string
    embedKey: string
    startKey: string
    endKey: string
    separator: string
    separatorKeyStart: string
    separatorKeyEnd: string
    separatorSymbol: string
}
